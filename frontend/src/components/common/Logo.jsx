import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className="text-2xl font-bold tracking-tight text-primary">
      Little Steps
    </Link>
  );
};

export default Logo;
